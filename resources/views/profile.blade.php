{{-- @extends('layouts.backend')

@section('content')
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">{{ __('Dashboard') }}</div>
                    <div class="card-body">
                        <div class="profile-show">
                            <h4>User Name: {{ $users->name }}</h4>
                            <h4>Profile Picture: </h4><img src="{{ asset('image/' . $users->profile_picture) }}" alt="Cover Image" width="100" style="border-radius: 50%">
                            <h4>Bio: {{ $users->bio }}</h4>
                            <h4>Created at: {{ $users->created_at }}</h4>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection --}}